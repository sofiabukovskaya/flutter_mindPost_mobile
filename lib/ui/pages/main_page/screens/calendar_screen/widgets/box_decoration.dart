import 'package:flutter/cupertino.dart';

BoxDecoration boxDecorationCalendar(Color color, BoxShape shape, BorderRadius radius){
  return BoxDecoration(
    color: color,
    shape: shape,
    borderRadius: radius,
  );
}